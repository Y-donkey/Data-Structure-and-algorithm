容器分为三大类：
	
	1）顺序容器：
		1：vector :后部插入/删除，直接访问
        2：deque:前/后部插入/删除，直接访问
        3: list：双向链表，任意位置插入/删除
    2) 关联容器：
    	1：set：快速查找，无重复元素
    	2：multiset ：快速查找，可有重复元素
    	3：map：一对一映射，无重复元素，基于关键字查找
    	4:multimap ：一对一映射，可有重复元素，基于关键字查找
    3) 容器适配器：
    	1：stack:LIFO
    	2：queue：FIFO
    	3: priority_queue：优先级高的元素先出 


   set/multiset/map/multimap 四种容器通常以平衡二叉树（红黑树）方式实现，插入和检索的时间都是 O(logN)

   为何map和set的插入删除效率比用其他序列容器高？
   	答：因为对于关联容器来说，不需要做内存拷贝和内存移动。只需要改变指针指向即可

   在set中查找是使用二分查找，效率非常高

  比较两个容器的大小：
  		若两容器长度相同、所有元素相等，则两个容器就相等，否则为不等。
		若两容器长度不同，但较短容器中所有元素都等于较长容器中对应的元素，则较短容器小于另一个容器
		若两个容器均不是对方的子序列，则取决于所比较的第一个不等的元素

